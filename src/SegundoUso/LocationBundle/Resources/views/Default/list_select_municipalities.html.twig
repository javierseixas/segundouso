{% for municipality in municipalities %}
<option value="{{ municipality.id }}" {% if app.session.get('currentMunicipality') == municipality.slug %}selected{% endif %}>{{ municipality.name }}</option>
{% endfor %}