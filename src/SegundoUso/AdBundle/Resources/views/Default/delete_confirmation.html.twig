{% extends 'SegundoUsoFrontendBundle::layout.html.twig' %}

{% block content %}

    <div class="row">
        {% for flashMessage in app.session.flashbag.get('alert-success') %}
            <div class="col-md-12">
                <div class="alert alert-success">{{ flashMessage }}</div>
            </div>
        {% endfor %}
        <div class="col-md-12">
            <p>Gracias por haber anunciado lo que ya no utilizas en SegundoUso.org.</p>
            <p>Si quieres seguir la experiencia de la reutilización puedes <a href="{{ path('segundo_uso_frontend_homepage') }}">buscar objetos</a> o <a href="{{ path('segundo_uso_frontend_ad_create') }}">publicar otro anuncio</a>.</p>
        </div>
    </div>

{% endblock content %}
